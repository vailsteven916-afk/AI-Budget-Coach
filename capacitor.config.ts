import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aibudgetcoach.app',
  appName: 'AI Budget Coach',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
